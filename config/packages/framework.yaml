framework:
    secret: '%env(APP_SECRET)%'
    session:
        enabled: true
        storage_factory_id: session.storage.factory.native
        cookie_secure: auto
        cookie_samesite: lax
    php_errors:
        log: true
